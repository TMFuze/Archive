<Page x:Class="Archive.Pages.UserPages.UserHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Archive.Pages.UserPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserHistoryPage">

    <Grid>
        <DataGrid Name="UserHistoryGrid" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер документа" Binding="{Binding Document.Number}" />
                <DataGridTextColumn Header="Дата получения" Binding="{Binding DateOfIssue}" />
                <DataGridTextColumn Header="Дата возврата" Binding="{Binding ReturnDate}" />
                <DataGridTextColumn Header="Примечание" Binding="{Binding Note}"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding User.FIO}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
